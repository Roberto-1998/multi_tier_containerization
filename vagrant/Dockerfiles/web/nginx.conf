# This is a simple Nginx configuration file for a web application.

worker_processes 1;

events {
  worker_connections 1024;
}

http {
  upstream vproapp {
    server vproapp01:8080;
  }

  server {
    listen 80;

    location / {
      proxy_pass http://vproapp;
    }
  }
}